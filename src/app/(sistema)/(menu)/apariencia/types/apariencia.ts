export interface AparienciaOption {
  value: string;
  label: string;
}

export interface ColorSwatchItem {
  id: string;
  color: string;
  label: string;
}

export interface ComprobanteToggleItem {
  id: string;
  label: string;
  enabled: boolean;
}
